// API Configuration for nginx proxy setup
export const API_BASE_URL = '/api';

export const API_ENDPOINTS = {
  GENERATE_ITINERARY: `${API_BASE_URL}/generate-itinerary`,
};

export default API_BASE_URL;
